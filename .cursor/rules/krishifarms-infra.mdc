---
description: KrishiFarms infra repo — read AGENTS.md and docs/INDEX.md before any edit
globs:
  - "**/*"
alwaysApply: true
---

# KrishiFarms infra — agent context

1. **Read first:** `AGENTS.md` → `docs/INDEX.md` → task-specific doc in `docs/`
2. **After changes:** follow `.cursor/rules/documentation-maintenance.mdc`
3. **Never** create EC2, RDS, ECS, EKS, Aurora, OpenSearch without explicit user request
4. **Shared EC2:** Gamya `:8080`, KrishiFarms prod `:8081`, dev `:8082`, qa `:8083`
5. **AWS:** account `085863558134`, region `ap-south-1`, EC2 `i-0426cdc00ff15bfe9`
6. **App code** is in `gvsharma/krishifarms-crm`, not this repo
7. **Secrets:** never commit `terraform.tfvars` or `.env`
8. **Git:** never push to `main` — push feature branches only; user merges PR to trigger CI apply
